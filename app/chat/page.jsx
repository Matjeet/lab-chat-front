import ChatPage from '@/components/pages/ChatPage';

export const metadata = {
  title: 'Chat · Chat',
};

/** Ruta "/chat": conversación 1 a 1 conectada a chat-conversacion. */
const Page = () => <ChatPage />;

export default Page;
