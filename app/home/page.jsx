import HomePage from '@/components/pages/HomePage';

export const metadata = {
  title: 'Chat',
};

/** Ruta "/home": destino tras un inicio de sesión correcto. */
const Page = () => <HomePage />;

export default Page;
